CREATE DATABASE FIVERINGS;

CREATE TABLE USUARIO
		(USUARIO VARCHAR(15) NOT NULL,
         CPF VARCHAR(11) NOT NULL,
         EMAIL VARCHAR(35),
         SENHA VARCHAR(15),
         REDEFINESENHA CHARACTER(1), 
         PRIMARY KEY (USUARIO));